import 'package:flutter/material.dart';

class OverviewModel{
  int? id;
  String? icon;
  String? name;
  int? itemCount;
  Widget? screen;
  OverviewModel({this.id, this.icon, this.name, this.itemCount, this.screen});
}